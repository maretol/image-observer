# Folder Tree 実装仕様書 (Phase 1) — アーカイブ

> **この spec は Phase 4 (spec-classification.md) で廃止された。**
>
> フォルダツリー機能 (`features/folder-tree/`) は Phase 4 で「完全削除」が決定され、
> 実装も削除済み。Go 側の `internal/tree/` パッケージも削除されている。
> 設計詳細が必要な場合は `git log -- docs/spec-folder-tree.md` を参照。
